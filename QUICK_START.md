# 🚀 Быстрый старт

## Локальный запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск dev-сервера
```bash
npm run dev
```

### 3. Откройте в браузере
```
http://localhost:3000
```

## Хостинг (деплой)

### Вариант 1: Vercel (РЕКОМЕНДУЕТСЯ) ⭐

Vercel - лучший выбор для Next.js проектов. Бесплатный план включает:
- Автоматический деплой из GitHub
- SSL сертификат
- CDN
- Домены вида `your-project.vercel.app`

#### Шаги:

1. **Загрузите код в GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
   git push -u origin main
   ```

2. **Подключите к Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий
   - Vercel автоматически определит Next.js
   - Нажмите "Deploy"

3. **Готово!** 
   - Сайт будет доступен по адресу: `your-project.vercel.app`
   - Каждый push в GitHub автоматически обновит сайт

#### Настройка кастомного домена:
- В настройках проекта Vercel → Domains
- Добавьте свой домен
- Следуйте инструкциям по настройке DNS

---

### Вариант 2: Netlify

1. **Загрузите код в GitHub** (как выше)

2. **Подключите к Netlify:**
   - Зайдите на [netlify.com](https://netlify.com)
   - "Add new site" → "Import an existing project"
   - Выберите GitHub репозиторий
   - Настройки Build:
     - Build command: `npm run build`
     - Publish directory: `.next`
   - Нажмите "Deploy"

---

### Вариант 3: Собственный сервер (VPS)

#### Требования:
- Node.js 18+
- PM2 (для управления процессом)

#### Установка:

1. **На сервере:**
   ```bash
   # Клонируйте репозиторий
   git clone https://github.com/ваш-username/ваш-репозиторий.git
   cd ваш-репозиторий
   
   # Установите зависимости
   npm install
   
   # Соберите проект
   npm run build
   ```

2. **Установите PM2:**
   ```bash
   npm install -g pm2
   ```

3. **Запустите:**
   ```bash
   pm2 start npm --name "ftc-portal" -- start
   pm2 save
   pm2 startup
   ```

4. **Настройте Nginx** (опционально):
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       
       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

---

## Проблемы и решения

### Порт 3000 занят?
Измените порт:
```bash
npm run dev -- -p 3001
```

### Ошибки при сборке?
```bash
# Очистите кэш
rm -rf .next
npm run build
```

### Проблемы с картой (Leaflet)?
- Убедитесь, что Leaflet CSS загружается
- Проверьте консоль браузера на ошибки

---

## Полезные команды

```bash
# Разработка
npm run dev          # Запуск dev-сервера

# Продакшн
npm run build        # Сборка проекта
npm start            # Запуск продакшн-версии

# Линтинг
npm run lint         # Проверка кода
```

---

## Следующие шаги после деплоя

1. ✅ Настройте кастомный домен
2. ✅ Добавьте Google Analytics (опционально)
3. ✅ Настройте базу данных (Supabase, PostgreSQL)
4. ✅ Добавьте аутентификацию (NextAuth.js)
5. ✅ Настройте загрузку файлов (Cloudinary, AWS S3)

---

## Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера
3. Убедитесь, что все зависимости установлены

